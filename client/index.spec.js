/* Needs for Karma
const context = require.context('./app', true, /\.(js|ts|tsx)$/);
context.keys().forEach(context);
*/

// Test stub, in order to pass a empty file correctly
describe('Stub', () => {
  it('should be true', () => {
    expect(true).toBeTruthy()
  })
})
